
\subsection{How much of the text-price effect is location}
\label{sec:decomposition}

The erasure diagnostic of Section~\ref{sec:leace-12} establishes that the
embedding carries location, and carries it abundantly, since a held-out ridge
recovers the latitude-longitude pair from the raw representation at an $R^2$ that
climbs to one half in the largest market before the projection drives it to the
floor. Taken on its own that fact invites the deflationary reading, that a
treatment built from such a representation is geography in disguise and its
apparent price effect is spatial confounding waiting to be exposed. The inference
does not actually follow, because a representation can encode a confounder along
directions that have little to do with the direction in which it predicts the
outcome, and whether the two coincide is an empirical matter rather than something
an erasure $R^2$ can settle on its own. Answering it means holding the treatment
fixed and asking what the location it shares with price is worth to the
estimate.

The instrument for that question is a fixed-treatment decomposition in the manner
of \citet{gelbach2016covariates}, whose appeal is that the change in a coefficient
when a block of controls is added does not depend on the order in which the blocks
enter, so the share it assigns to geography is well defined rather than an
artifact of sequencing. We estimate the per-standard-deviation effect of the
leading text direction on log price twice, once controlling for the structured
confounders alone and once adding a flexible location basis of latitude,
longitude, their squares, and their interaction, and we read the gap between the
two as the portion of the effect that location explains. The comparison appears in
the left panel of Figure~\ref{fig:decomposition}, where the two estimates sit
almost on top of one another in eleven of the twelve markets, the location
adjustment moving the coefficient by at most about a hundredth of a log point
outside New York and by under five percent of its size wherever the effect is
non-trivial, with the per-market figures collected in
Appendix~\ref{app:decomposition}. Philadelphia, Chicago, Atlanta, and the other high-effect markets give up
essentially nothing to the geography control, the small-effect markets give up
nothing they had to begin with, and the per-market premium that the text channel
identifies comes through the adjustment very nearly untouched by whether the model
is told where each property sits.

A reader entitled to be skeptical will ask at once whether the test has any power,
since a treatment that carried no geography in the first place would pass it for
free, and the right panel of Figure~\ref{fig:decomposition} exists to meet that
objection before it hardens into one. The share of the treatment's own variance
that the location basis explains is nowhere near zero across the panel, reaching
$0.42$ in New York and exceeding a tenth in six markets, so the leading text
direction does in fact encode location through much of the country. That the price
effect survives adjustment even where the treatment is substantially geographic is
the whole of the result, because it means the geographic component of the language
is real, the model can see it, and it still does not carry the effect of the
language on price. The worry that has hung over this literature is therefore one
that can be answered rather than only raised, and the answer across eleven of
twelve markets is that the text is doing more than reading the map back to itself.

New York is where the adjustment does bite, and it bites in precisely the place
the diagnostic says it should. The market whose text direction is the most
geographic of the twelve, at an $R^2$ of $0.42$, is also the market where folding
in location moves the effect most, from $-0.119$ to $-0.152$, a shift of about a
quarter of its magnitude that leaves the sign intact, so that the place with the
most location in its language is the place that location matters most for the
estimate. We read this less as a crack in the broader finding than as its
mechanism caught in the act, and as a reminder that the protocol earns its keep by
flagging the markets where a spatial audit is most warranted rather than by
declaring that none is ever needed.

The market the diagnostic singles out also reads differently once one looks at the
language behind the direction. Interpreting the leading text direction by the
vocabulary that loads on it, in the way \citet{nowak2017textual} read implicit
prices off listing tokens and the construct-validity approach of
\citet{fang2022construct} formalises for embeddings, shows that what the New York
direction is built from is location. Its heaviest terms are boroughs and
neighborhoods and the transit that joins them, Manhattan and Brooklyn and Midtown
and SoHo, the subway and the avenue, alongside the housing nouns peculiar to the
city such as the apartment and the studio. The same exercise in a market the
adjustment leaves untouched returns condition and finish instead, so that in Dallas
the owner suite and the kitchen and the open dining space carry the high-price end
while the city and its suburbs load only at the low end. New York's listings locate
a property where Dallas's describe one, and a direction made of place names is
mechanically a direction that geography can move. That location is so heavily
written into the words is itself a property of the market, since where submarkets
are drawn tightly and fine-scale geography is steeply valued
\citep{goodman1998housing,bourassa1999defining} the name of a neighborhood is
already a statement about price, and the confounding such language carries sits at
exactly the spatial grain a coarse control cannot reach
\citep{paciorek2010importance,keller2020selecting}. The pattern is not that naming
places makes a market's text geographic in the sense that matters here. Phoenix
names its places as readily as New York, its axis carrying Scottsdale and Arcadia
and the desert, and yet its text effect is indistinguishable from zero and the
adjustment leaves it there, because a Phoenix suburb is not the steep price
discontinuity that a Manhattan block is. It is locational language where location
is steeply priced, rather than locational language as such, that the deconfounding
responds to.

\begin{table}[H]
\centering
\caption{The vocabulary of the leading text direction in the market the spatial
adjustment moves most and a market it leaves untouched. Description terms whose
TF-IDF most strongly loads on the price-oriented sentence-BERT direction. New
York's direction is built from boroughs, neighborhoods, and transit; Dallas's from
condition and finish, with the city and its suburbs loading only at the low-price
end. Per-market lists are produced by the replication code.}
\label{tab:nyc-vocab}
\small
\begin{tabular}{p{0.43\textwidth}p{0.43\textwidth}}
\toprule
New York & Dallas \\
\midrule
Manhattan, Brooklyn, Midtown, SoHo, Bensonhurst, subway, avenue, apartment, studio
& owner suite, kitchen, open dining, living space, overlooking, thoughtfully
curated, comfort \\
\bottomrule
\end{tabular}
\end{table}

The conclusion travels only as far as its
instruments reach, and they reach less far than one would like. The location
basis is a smooth coordinate polynomial laid over roughly thirty structured
controls that already carry census-tract demography, so confounding operating at a
finer spatial grain than the polynomial can bend to, or running through pathways
those controls do not capture, lies outside what the comparison can see. And the
erasure that certifies the representation's geography is itself linear, so a
nonlinear geographic signal no ridge probe would surface could in principle
persist into the residual we are calling semantic.

That concession invites a direct test, and we run it by replacing the coordinate
polynomial with a thin-plate regression spline, the flexible low-rank smoother
that the spatial-statistics literature reaches for when the scale of confounding
is not known in advance \citep{keller2020selecting,gilbert2021causal}. A
rank-thirty spline over latitude and longitude, projected to meters so that its
isotropic metric is physical, captures far more of the treatment's spatial
structure than the quadratic does, raising the average share of the leading text
direction that location explains from about $0.13$ to about $0.21$ and from $0.42$
to $0.52$ in New York, and yet the price effect after adjustment barely responds.
The location-adjusted estimate moves by at most $0.059$ of a log point across the
twelve markets, in New York, and by under $0.03$ in every other market, with no
market of detectable effect changing sign and the pooled effect shifting from
$+0.129$ to $+0.121$. New York aside, a basis flexible enough to absorb twice the
geography leaves the semantic estimate essentially where the quadratic left it; in
New York the finer grain takes a little more, moving the estimate by about
$0.059$, still without overturning it. The finer spatial grain the
polynomial could not bend to turns out, where the data can see it, not to carry
the price effect either, and the per-market comparison is released with the
replication code.

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
    causalre, width=0.42\textwidth, height=6.6cm, name=dcA,
    symbolic y coords={New York,Phoenix,Seattle,Denver,Portland,San Francisco,Atlanta,Boston,Washington,Dallas,Philadelphia,Chicago},
    ytick=data, enlarge y limits=0.06,
    xlabel={$\hat\theta$ per $\sigma$ (log price)}, xmin=-0.21, xmax=0.53,
    xtick={-0.1,0,0.2,0.4},
    title={(a)},
    extra x ticks={0}, extra x tick style={grid=major, grid style={black, dashed, thin}}, extra x tick labels={},
    legend style={font=\scriptsize, draw=none, fill=none, at={(0,1.13)}, anchor=south west, legend columns=2, /tikz/every even column/.append style={column sep=10pt}},
]
\draw[thin,gray] (axis cs:-0.122,New York)--(axis cs:-0.154,New York);
\draw[thin,gray] (axis cs:0.004,Phoenix)--(axis cs:-0.009,Phoenix);
\draw[thin,gray] (axis cs:0.046,Seattle)--(axis cs:0.044,Seattle);
\draw[thin,gray] (axis cs:0.067,Denver)--(axis cs:0.082,Denver);
\draw[thin,gray] (axis cs:0.076,Portland)--(axis cs:0.085,Portland);
\draw[thin,gray] (axis cs:0.122,San Francisco)--(axis cs:0.119,San Francisco);
\draw[thin,gray] (axis cs:0.124,Atlanta)--(axis cs:0.129,Atlanta);
\draw[thin,gray] (axis cs:0.126,Boston)--(axis cs:0.125,Boston);
\draw[thin,gray] (axis cs:0.129,Washington)--(axis cs:0.120,Washington);
\draw[thin,gray] (axis cs:0.146,Dallas)--(axis cs:0.147,Dallas);
\draw[thin,gray] (axis cs:0.367,Philadelphia)--(axis cs:0.363,Philadelphia);
\draw[thin,gray] (axis cs:0.463,Chicago)--(axis cs:0.472,Chicago);
\addplot[only marks, mark=o, mark size=2.2pt, color=cMUTE, line width=0.9pt] coordinates {
  (-0.122,New York) (0.004,Phoenix) (0.046,Seattle) (0.067,Denver) (0.076,Portland)
  (0.122,San Francisco) (0.124,Atlanta) (0.126,Boston) (0.129,Washington) (0.146,Dallas)
  (0.367,Philadelphia) (0.463,Chicago)};
\addlegendentry{without location}
\addplot[only marks, mark=*, mark size=1.7pt, color=cNYC] coordinates {
  (-0.154,New York) (-0.009,Phoenix) (0.044,Seattle) (0.082,Denver) (0.085,Portland)
  (0.119,San Francisco) (0.129,Atlanta) (0.125,Boston) (0.120,Washington) (0.147,Dallas)
  (0.363,Philadelphia) (0.472,Chicago)};
\addlegendentry{with location}
\end{axis}
\begin{axis}[
    causalre, width=0.24\textwidth, height=6.6cm,
    at={(dcA.east)}, anchor=west, xshift=0.7cm,
    symbolic y coords={New York,Phoenix,Seattle,Denver,Portland,San Francisco,Atlanta,Boston,Washington,Dallas,Philadelphia,Chicago},
    ytick=data, yticklabels={,,,,,,,,,,,},
    xlabel={PC1 geography $R^2$}, xmin=0, xmax=0.46, xtick={0,0.2,0.4},
    enlarge y limits=0.06,
    title={(b)},
]
\addplot[only marks, mark=square*, mark size=1.8pt, color=cSF] coordinates {
  (0.419,New York) (0.097,Phoenix) (0.035,Seattle) (0.164,Denver) (0.161,Portland)
  (0.035,San Francisco) (0.148,Atlanta) (0.054,Boston) (0.120,Washington) (0.059,Dallas)
  (0.117,Philadelphia) (0.187,Chicago)};
\end{axis}
\end{tikzpicture}
\caption{Is the per-market text-price effect spatially confounded? Holding the
leading text principal component fixed as the treatment, panel (a) plots its DML
effect on log price controlling for the structured confounders without (open) and
with (filled) a location adjustment that adds latitude, longitude, and their
quadratic and interaction terms to the confounder set. The two estimates nearly
coincide in eleven of twelve markets, with the location adjustment moving the
estimate by under $0.02$ of a log point; New York is the exception, where it moves
the estimate by about a quarter of its magnitude, $0.032$, without changing its
sign. Panel (b) reports the share of the treatment's own variance that the
location basis explains, which reaches $0.42$ in New York and exceeds $0.10$ in
seven markets, so
the robustness in panel (a) is not an artifact of a treatment that carries no
geography: the text direction does encode location, yet that location component
does not drive its price effect. Panel (a) plots point estimates; in every market
the shift from the unadjusted to the location-adjusted estimate is small relative
to the estimate's own standard error, so the near-coincidence does not mask a
noisy difference.}
\label{fig:decomposition}
\end{figure}
