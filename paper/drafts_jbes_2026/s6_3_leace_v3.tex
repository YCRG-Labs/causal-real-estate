
\subsection{How much location does the representation encode?}
\label{sec:leace-12}

Before asking whether the text effect survives location, we check the
precondition that makes the question worth asking at all: that the representation
encodes location to begin with. The sentence-BERT representation that underlies
the Baur channel in Section~\ref{sec:baur-12city} encodes more than the language
a listing agent uses. Pre-trained on a generic corpus, the encoder has no particular incentive
to separate semantic content about a property's condition or amenities from the
geographic information that correlates with its neighborhood. If the embedding
carries location-targeted information into the leading principal component, the
per-standard-deviation effect reported in Section~\ref{sec:baur-12city} inherits
a spatial confounder mediated through the encoder. The identification claim
is correspondingly weaker. We assess how far this concern can be addressed by
erasing the lat-lon concept from the representation. We report where the erasure
is complete and where it is only partial.

The deconfounding runs a linear projection and then a nonlinear correction, and
the linear step carries most of the load. Applying the LEACE projection of
\citet{belrose2023leace} to the BERT embedding removes the linear subspace that
the mean-conditional-covariance estimator identifies with the concept variable,
here the latitude-longitude coordinate pair of the listing. It comes with the
exact-guardedness guarantee that no linear predictor of the residual embedding can
recover the concept any better than a constant baseline would. That guarantee
holds in every market we examine. The out-of-fold ridge $R^2$ for predicting
the coordinate from the embedding falls from a raw value that reaches $0.50$ in
New York and $0.42$ in Dallas to within $0.002$ of zero after erasure. The
linear-guardedness residual averages $2.7\text{e-}8$ across the twelve markets,
ranging from $8.3\text{e-}9$ to $6.0\text{e-}8$, which is the floating-point floor
of the single-precision computation. The linear erasure therefore comes out complete
throughout the panel (Figure~\ref{fig:erasure-12}).

The concept is routed through the continuous coordinate representation rather
than the categorical ZIP-code identifier in every market. Categorical LEACE has
known instability when the categorical cardinality is large, and our panel ranges
from $30$ ZIP codes in San Francisco to $158$ in New York. The per-market test
folds are now large, from $296$ listings in San Francisco to
$4{,}569$ in New York. Even so, the continuous routing sidesteps the cardinality question
entirely without discarding the spatial information the projection is designed to
remove, since the coordinate pair is a sufficient statistic for location at the
resolution that matters here.

What a linear projection cannot reach is the location the embedding encodes
nonlinearly. The iterative gradient-based correction of
\citet{iskander2023shielded} is aimed at exactly that residual. It alternates between fitting
a multilayer-perceptron probe of the coordinate on the residual and projecting out
the direction in which that probe gathers its predictive mass. The correction cuts
the nonlinear leakage substantially without removing it, taking the average
nonlinear probe $R^2$ from $0.44$ on the raw embedding down to $0.20$ after five
outer passes, a reduction of roughly a half. The residual that remains
varies sharply across markets. San Francisco is left with a nonlinear $R^2$ of
$0.01$, which is erasure for any practical purpose, whereas New York, Dallas, and
Phoenix hold onto residuals near $0.30$ to $0.34$ and Portland near $0.26$. The
markets that keep the most are the larger corpora whose listing language
carries the richest location-targeted structure. This is also where a nonlinear
probe has the most left to find.

\paragraph{Why closed-form erasure rather than adversarial training.} We prefer
the closed-form projection to the adversarial alternative the disentanglement
literature more often reaches for, for a reason we develop in
Appendix~\ref{app:adversarial}. An adversarially trained encoder can drive its own
location discriminator to chance and still leave the coordinate recoverable. A
fresh probe of the frozen representation reads latitude and longitude back at an
$R^2$ between $0.16$ and $0.42$ across the twelve markets, and the ZIP code at nine
to forty-seven times the uniform rate. The adversarial training does not remove
location; it teaches its discriminator not to look. The closed-form projection, by
contrast, is guarded against any probe by construction. It is the erasure on
which we rest the decomposition that follows.

\paragraph{What the diagnostic does and does not settle.} The Baur estimates of
Table~\ref{tab:baur-12city} are computed on the unprojected embedding, in keeping
with the published Baur, Rosenfelder, and Lutz protocol. The erasure here is a
diagnostic of what the representation contains rather than a change to any
per-market number. As a diagnostic it speaks directly to the part of the problem
the linear treatment can actually express. The leading principal component
is a linear functional of the embedding, and the linear erasure is exact in every
market. Whatever location a linear treatment could carry has been shown to be
removable in full, while the nonlinear residual that survives IGBP in the larger
markets bounds the rest, fixing how much location a nonlinear function of the
deconfounded representation could still smuggle back. Showing that the embedding
encodes geography is not, however, the same as showing that the price effect is
confounded by it, and the diagnostic on its own cannot close that gap. A
representation may carry a confounder along directions that have nothing to do
with the one on which it acts. The question of whether the location the embedding
demonstrably encodes is the location that actually moves the price is taken up
directly in Section~\ref{sec:decomposition}, where the text treatment is held
fixed and geography is moved on and off the controls around it.

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
    causalre, width=0.66\textwidth, height=6.8cm,
    symbolic y coords={San Francisco,Chicago,Seattle,Philadelphia,Portland,Boston,Atlanta,Washington,Denver,Phoenix,Dallas,New York},
    ytick=data, enlarge y limits=0.06,
    xlabel={Held-out ridge $R^2$ for predicting (lat, lon) from the embedding},
    xmin=-0.42, xmax=0.56, xtick={-0.4,-0.2,0,0.2,0.4},
    extra x ticks={0}, extra x tick style={grid=major, grid style={black, dashed, thin}}, extra x tick labels={},
    legend style={font=\scriptsize, draw=none, fill=none, at={(0,1.02)}, anchor=south west, legend columns=2, /tikz/every even column/.append style={column sep=10pt}},
]
\draw[thin,gray] (axis cs:-0.330,San Francisco)--(axis cs:-0.0019,San Francisco);
\draw[thin,gray] (axis cs:0.165,Chicago)--(axis cs:0.0000,Chicago);
\draw[thin,gray] (axis cs:0.215,Seattle)--(axis cs:-0.0017,Seattle);
\draw[thin,gray] (axis cs:0.239,Philadelphia)--(axis cs:-0.0008,Philadelphia);
\draw[thin,gray] (axis cs:0.287,Portland)--(axis cs:-0.0001,Portland);
\draw[thin,gray] (axis cs:0.348,Boston)--(axis cs:-0.0006,Boston);
\draw[thin,gray] (axis cs:0.350,Atlanta)--(axis cs:-0.0015,Atlanta);
\draw[thin,gray] (axis cs:0.357,Washington)--(axis cs:-0.0003,Washington);
\draw[thin,gray] (axis cs:0.377,Denver)--(axis cs:-0.0009,Denver);
\draw[thin,gray] (axis cs:0.380,Phoenix)--(axis cs:-0.0014,Phoenix);
\draw[thin,gray] (axis cs:0.423,Dallas)--(axis cs:-0.0005,Dallas);
\draw[thin,gray] (axis cs:0.500,New York)--(axis cs:-0.0004,New York);
\addplot[only marks, mark=o, mark size=2.3pt, color=cMUTE, line width=0.9pt] coordinates {
  (-0.330,San Francisco) (0.165,Chicago) (0.215,Seattle) (0.239,Philadelphia) (0.287,Portland)
  (0.348,Boston) (0.350,Atlanta) (0.357,Washington) (0.377,Denver) (0.380,Phoenix)
  (0.423,Dallas) (0.500,New York)};
\addlegendentry{Raw embedding}
\addplot[only marks, mark=*, mark size=2.1pt, color=cNYC] coordinates {
  (-0.0019,San Francisco) (0.0000,Chicago) (-0.0017,Seattle) (-0.0008,Philadelphia) (-0.0001,Portland)
  (-0.0006,Boston) (-0.0015,Atlanta) (-0.0003,Washington) (-0.0009,Denver) (-0.0014,Phoenix)
  (-0.0005,Dallas) (-0.0004,New York)};
\addlegendentry{After LEACE (linear erasure)}
\end{axis}
\end{tikzpicture}
\caption{Held-out ridge $R^2$ for recovering the continuous latitude-longitude
coordinate from the sentence-BERT embedding, before (open) and after (filled) the
LEACE linear concept erasure, in each of the twelve markets ordered by raw $R^2$.
The raw embedding carries substantial linear spatial signal, recovering the
coordinate at $R^2$ up to $0.50$ in New York and $0.42$ in Dallas; the negative San
Francisco value reflects out-of-sample probe performance below the coordinate-mean
baseline rather than absent signal, on that market's small held-out fold of $296$
listings. Linear erasure drives the recoverable signal to within $0.002$ of zero in
every market, the filled points sitting on the zero line, which is the
exact-guardedness property of LEACE read through a fresh out-of-fold probe rather
than the guardedness residual itself. Residual \emph{nonlinear} leakage, which a
linear probe cannot see, is quantified separately in Section~\ref{sec:leace-12}.}
\label{fig:erasure-12}
\end{figure}

