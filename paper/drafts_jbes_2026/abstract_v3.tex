% Abstract, v4 (AOAS-targeted reframe, 2026-06-07). Leads with the two
% methodological contributions the framing research identified as the defensible
% hooks -- (i) predictive fit cannot separate semantic from spatial signal but
% concept erasure can, framed as representation-space restricted spatial
% regression; (ii) DML influence-function SEs undercover for an embedding-valued
% generated-regressor treatment, with a bootstrap remedy -- and reports the
% honest applied findings second. NOT a causal price elasticity. Numbers locked
% against the canonical files / v3 body. One paragraph, flowing prose, no em
% dashes, no enumerations, no fragments.
\begin{abstract}

Automated valuation models increasingly read the prose of a property listing, and
the text reliably improves price prediction, yet predictive fit alone cannot
reveal whether the gain reflects genuine semantic content or the neighborhood
geography that language quietly encodes. We treat the listing embedding as a
high-dimensional treatment and develop a framework that asks how much of its
association with sale price survives once geographic location is erased from the
representation, joining a Robinson partially-linear Double Machine Learning
estimand with cross-fitted gradient-boosted nuisances, linear concept erasure of
the latitude-longitude coordinate in closed form, and an iterative nonlinear
erasure that exposes the residual leakage a linear projector leaves behind. Two
methodological findings anchor the framework. Erasing the spatial coordinate from
the representation is a representation-space analogue of restricted spatial
regression, and where predictive fit cannot tell semantic content apart from
spatial information, concept erasure can. And the influence-function standard
error that Double Machine Learning reports for an embedding-valued,
generated-regressor treatment undercovers at the sample sizes typical of a single
metropolitan market, an effect we document in a calibration study and for which we
recommend a bootstrap that resamples the entire pipeline. Applying the framework
across twelve metropolitan areas with $3{,}851$ deduplicated Redfin listings, we
find that text carries a positive per-standard-deviation association with log
price that is robust to the choice of encoder, strongly heterogeneous across
markets, fully erasable in its linear spatial component while a nonlinear
remainder persists where the city is large, and unaccompanied by any demographic
moderator that survives multiplicity and leverage diagnostics. We are explicit
about what this does and does not establish, because the estimates are
associational rather than cleanly causal and a sensitivity analysis quantifies how
weak an unmeasured confounder would need to be to overturn each market's estimate.
The contribution is a disciplined measurement-and-deconfounding protocol with
calibrated inference for an embedding treatment under spatial confounding, not a
causal price elasticity.

\end{abstract}
